# 06 — Decisions Log

Every significant design choice with alternatives considered and reversibility assessment.

| # | Decision | Alternatives considered | Chosen because | Reversibility |
|---|---|---|---|---|
| D-01 | `CaptureActor` (was `CameraEngine`) as Swift serial `actor` in `CaptureKit` (not class + serial queue). Owns `AVCaptureSession` and device KVO. | (A) class + `DispatchQueue` serial queue + explicit locks; (B) class + OSLock | Swift actor provides compiler-enforced isolation — domain Invariant 1 (camera state exclusively serialized) is a compile-time guarantee, not a runtime promise. Actor re-entrancy at `await` points is well-understood and controlled. Serial queue approach requires manual discipline; actors make violations impossible to express. | Low — changing to a queue-based model would require significant refactoring of all caller sites that use `await actor.method()`. Worth doing only if actor re-entrancy causes subtle state bugs. |
| D-02 | VTFrameProcessor: **not used**; custom Metal compute shaders chosen. **Re-checked 2026-04-16** against WWDC25 Video Toolbox sessions now that iOS 26 is pinned — verdict confirmed. | (A) `VTFrameProcessor` (iOS 26 VideoToolbox API); (B) CoreImage filter chain | `VTFrameProcessor` supports system-defined effects (motion deblur, super-resolution, noise reduction, frame interpolation) — it does not expose arbitrary per-channel color transform pipelines. The required 5-stage color pipeline (black balance → brightness → contrast → saturation → gamma with Rec.709 coefficients) cannot be expressed through `VTFrameProcessor`'s effect enumeration. CoreImage filters would work but add overhead (NSObject-based pipeline, implicit texture copies) and the filter graph is harder to instrument. `VTFrameProcessor` is the right tool for apps that want Apple-authored effects; it is the wrong tool for apps that own their shader pipeline. Custom Metal compute kernels are fast, inspectable, and directly instrumentable. | Medium — if Apple adds custom color pipeline support to `VTFrameProcessor` in a future SDK, it would be worth revisiting. |
| D-03 | `AVAssetWriter` + `AVAssetWriterInputPixelBufferAdaptor` for video recording with true GPU-to-encoder zero-copy via IOSurface-backed `CVPixelBuffer` pool | (A) `AVCaptureMovieFileOutput`; (B) VideoToolbox `VTCompressionSession` directly; (C) `AVAssetWriter` with `MTLTexture.getBytes` CPU blit | `AVCaptureMovieFileOutput` cannot encode GPU-processed frames — it writes the sensor's capture output directly, bypassing the Metal pipeline. `VTCompressionSession` adds unnecessary complexity (manual sample buffer construction, manual atom/moov management). `AVAssetWriter` + `AVAssetWriterInputPixelBufferAdaptor` is the standard iOS path. Zero-copy is achieved by (1) creating the pixel buffer pool with `kCVPixelBufferIOSurfacePropertiesKey` and `kCVPixelBufferMetalCompatibilityKey: true`, (2) wrapping each dequeued `CVPixelBuffer` as a `MTLTexture` via `CVMetalTextureCache`, (3) blitting the processed output into the wrapped texture with `MTLBlitCommandEncoder` (GPU-local, stays in IOSurface memory), (4) appending the pixel buffer to the adaptor — VideoToolbox maps the same IOSurface for encoding. `MTLTexture.getBytes` is **explicitly forbidden** on this path — it would violate the domain zero-copy invariant. Full implementation pattern documented in `03-metal-pipeline.md §GPU-to-Encoder Path`. | Low — `AVAssetWriter` is the standard iOS high-quality recording path. The IOSurface zero-copy integration is non-negotiable; alternatives all copy. |
| D-04 | ~~ObjC++ bridge for OpenCV; direct Swift-C++ interop for `IFrameConsumer`~~ **SUPERSEDED by D-16** | ~~(A) Pure direct Swift-C++ interop for all C++ code; (B) Pure ObjC++ wrapper for all C++ code~~ | ~~OpenCV headers use templates, macros, and C++ exceptions that Swift's Clang module importer cannot bridge. Two-layer approach gives the cleanest public API…~~ **Historical context preserved:** this decision predated the insight that OpenCV headers only cause interop pain when Swift *imports* them. Under D-16, OpenCV is fully encapsulated in `Sources/ImagingCore/src/*.cpp`, so Swift never imports `<opencv2/*>` and the whole ObjC++ workaround becomes unnecessary. | Superseded. |
| D-05 | ~~OpenCV distributed as **xcframework** (pre-built)~~ **REFINED by D-19** | (A) CocoaPods; (B) SPM; (C) Build from source | ~~Pre-built xcframework, "drag into Xcode" integration, `HEADER_SEARCH_PATHS` via `.xcconfig`.~~ Superseded by **D-19**: the same `opencv2.xcframework` is now wrapped as a SwiftPM `binaryTarget` so it integrates through the package graph rather than a hand-wired xcconfig. This makes it compose with `ImagingCore` and removes the `EXCLUDED_ARCHS` hack. | Refined. |
| D-06 | Edge detection result type: **EdgeResult object** containing status, contour list, framePTS, and processingTime. **Edge visualization is NOT contour rendering** — C++ EdgeDetector composites edges onto the tracker image in C++, writes the composited result to a shared MTLTexture. The contour list in EdgeResult is for future features (ROI labels, hover, annotation export). See D-24. | (A) Binary edge mask (`CVPixelBuffer`); (B) Edge coordinate list per-row; (C) Contour list only, rendered via SwiftUI Canvas (original design) | The original approach (contour list rendered via SwiftUI Canvas `Path`) worked but sat above the Metal view with layering seams. Under D-24, edge visualization is done in C++ (compositing onto the tracker image) and displayed via a pan/zoom Metal quad. The contour list is retained in EdgeResult as metadata for future features — it's cheap to compute alongside Canny. | Low — the visualization path (D-24) and the data export path (contour list) are independent; either can change without affecting the other. |
| D-07 | Split-screen preview: two separate `MTKView` instances | (A) Single `MTKView` with side-by-side blit; (B) `MTKView` + `UIImageView` | Two `MTKView` instances map cleanly to the two SwiftUI `UIViewRepresentable` wrappers. Each renderer is independently updateable. Single `MTKView` with two-region blit adds coordinate math complexity. `UIImageView` requires a CPU-side pixel buffer transfer for the natural stream, which is unnecessary. | Low — architectural; both views share the same Metal device, so combining is possible later. |
| D-08 | App lifecycle "fully invisible" → `scenePhase == .background` (not `.inactive`) | (A) `scenePhase == .inactive`; (B) UIApplicationDelegate `applicationWillResignActive` | Domain spec explicitly requires "fully invisible" trigger to avoid releasing camera on partial occlusion (dialog overlay, notification banner). SwiftUI `scenePhase == .inactive` fires on partial occlusion. `.background` fires only when the app is fully off-screen — exact match for domain intent. This is a direct resolution of U-07. | Low — this is the correct behavior per the domain spec; no reason to revisit. |
| D-09 | Settings persistence: **UserDefaults + Codable** | (A) Core Data; (B) SQLite; (C) Keychain | `CameraSettings` and `ProcessingParameters` are small Codable structs. `UserDefaults` is zero-setup, supports `Double` natively (no bit-packing workaround needed), and is appropriate for user-facing preferences. Core Data and SQLite are over-engineered for this use case. Keychain is for secrets. | Low — if settings grow significantly, migration to Core Data is straightforward. |
| D-10 | ~~`ConsumerRegistry` as a separate Swift `actor` from `CameraEngine`~~ **SUPERSEDED by D-23** | (A) Consumer registry inside `CameraEngine` actor | ~~Separating the registry from the engine allows consumers to be registered/unregistered without holding the engine actor.~~ **Superseded:** consumer dispatch is now entirely inside C++ via the `PixelSink` thread pool (D-23). The Swift `ConsumerRegistry` actor is removed. | Superseded. |
| D-11 | Camera orientation: use `AVCaptureConnection.videoRotationAngle` | (A) Manual UV transform matrix in Metal shader (as Android source did) | iOS AVFoundation handles sensor orientation via `AVCaptureConnection.videoRotationAngle`. Setting the correct rotation at session configuration time means the `CVPixelBuffer` arrives pre-rotated — no Metal shader UV matrix needed. This is simpler, more correct, and doesn't require re-determining the rotation angle per-frame. | Low — if AVFoundation rotation produces incorrect results for the target hardware, the Metal approach can be restored. |
| D-12 | `AsyncStream<T>` for state, error, frameResult delivery to ViewModel | (A) Combine `PassthroughSubject`; (B) Direct `@MainActor` method calls from engine | Swift concurrency `AsyncStream` is the idiomatic iOS 26 / Swift 6 approach. No Combine dependency. `for await` loops in `.task { }` modifiers auto-cancel on view disappear. Direct `@MainActor` calls from `CameraEngine` would create stronger coupling between engine and UI layer. | Medium — replacing with Combine is possible but adds a framework dependency; stick with AsyncStream. |
| D-13 | Focus distance: iOS `lensPosition` (0–1 normalized); **diopter display approximated** | (A) Attempt hardware-specific diopter-to-lensPosition calibration curve; (B) Expose raw `lensPosition` without diopter framing | iOS `AVCaptureDevice` uses `lensPosition` (0.0 = near, 1.0 = far), not diopters. Exact diopter conversion requires factory calibration data not available at design time. Design exposes `lensPosition` in manual focus control; the UI labels it as a relative focus control rather than diopters. The domain API contract preserves the `focusDistanceDiopters` field name for cross-platform compatibility, but the iOS value is the normalized position. This is documented as a known deviation in `07-ios-specific-risks.md`. | Medium — if hardware calibration data becomes available, the conversion function can be added without API change. |
| D-14 | ~~SDR only — `BGRA8Unorm` throughout~~ **SUPERSEDED by D-18** | (A) `rgba16Float` for HDR processing | ~~The domain color pipeline is SDR-range (brightness `[-1,1]`, contrast `[0,2]`). All consumers receive `RGBA8888` per domain spec.~~ **Superseded:** the pipeline now uses `kCVPixelFormatType_64RGBAHalf` / Metal `.rgba16Float` end-to-end, and consumers receive `FrameDataPixelFormat::RGBAHalf16`. The motivation is not HDR; it is precision preservation across the 5-stage color-transform chain, which compounds banding at 8-bit. Half-float math runs full-rate on Apple Silicon. See D-18. | Superseded. |
| D-15 | `sampleCenterPatch()` reads from Metal readback buffer (CPU-side) | (A) Dispatch a separate Metal compute pass to sample the patch; (B) Read from `MTKView.currentDrawable` | The readback buffer is already a CPU-accessible RGBA buffer. Reading a 96×96 window from it is trivial and runs on the actor — no additional GPU work needed. The histogram trimmed mean computation in Swift/CPU on 9,216 pixels takes < 1ms. Separate GPU pass adds command buffer overhead for no benefit at this size. | Low — if 96×96 proves insufficient, the patch size is a single constant change. |
| D-16 | **Pure Swift ↔ C++ interop, no Objective-C++.** OpenCV is a private implementation detail of `ImagingCore`. | (A) Hybrid: direct interop for simple types + `.mm` for OpenCV (old D-04); (B) Pure ObjC++ for everything; (C) Pure C-ABI shim | Under Swift 6.2+ with `.interoperabilityMode(.Cxx)` and `cxxLanguageStandard: .cxx20`, the Clang module importer handles POD, `std::span`, and enum classes cleanly. OpenCV's problematic headers only cause pain if Swift *imports* them — and Swift never does here, because `<opencv2/opencv.hpp>` is banned from every public header under `Sources/ImagingCore/include/` and only legal inside `.cpp` files. The result is a single-layer interop story with no `.mm` files anywhere in the project. Supersedes D-04. | Reversible — re-adding a `.mm` layer is a localized change that does not affect `ImagingCore`'s public interface. |
| D-17 | **`Sources/<TargetName>/` SwiftPM layout** with a thin `App/` shell. `ImagingCore` is a pure-C++ SwiftPM target with no Apple-framework dependencies in its public headers. | (A) Single-target Xcode project with folder groups; (B) Mixed Xcode + SwiftPM hybrid | SwiftPM library targets are enforced by the build system: `ImagingCore` cannot accidentally import `UIKit` because the target declaration does not list `UIKit` as a dependency. This is strictly stronger than Xcode folder groups, which only organize files visually. The Apple-free public header surface is what makes `ImagingCore` **independently testable** — `ImagingCoreTests` can run `EdgeDetectionConsumer` against in-memory fixtures via `swift test` on the macOS host without standing up a camera, Metal, or AVFoundation. `App/` remains a thin shell holding only `@main`, `Info.plist`, and assets — everything that *could* live in a library *does*. A CMake/Linux build for `ImagingCore` is out of scope for 2026-04-16; it could be added later if non-Apple portability becomes a goal. | Low — restructuring back to a flat layout would require rewriting `Package.swift` but not touching any source file's import statements. |
| D-18 | **Capture format is 8-bit YUV** (`kCVPixelFormatType_Lossless_420YpCbCr8BiPlanarFullRange`); **working format is RGBA16F** (`rgba16Float`). **REFINED from original (which incorrectly specified `_64RGBAHalf` as the capture format).** | (A) `kCVPixelFormatType_32BGRA` 8-bit RGBA (wastes bandwidth; device converts internally); (B) `kCVPixelFormatType_64RGBAHalf` half-float RGBA from capture (device does NOT support this format on AVCaptureVideoDataOutput — verified on A16 hardware); (C) `kCVPixelFormatType_420YpCbCr10BiPlanarFullRange` 10-bit YUV (device does NOT support 10-bit or 12-bit output — verified on A16 hardware) | **Capture:** lossless hardware-compressed 8-bit YCbCr 4:2:0 full range is the most data-efficient format the device supports. Hardware compression reduces memory bandwidth vs uncompressed 8-bit YUV with no fidelity loss. AVFoundation delivers this as IOSurface-backed biplanar buffers (Y plane + CbCr plane). **Working:** Metal Pass 1 converts YUV → RGBA16F via BT.709 matrix in a compute kernel. All subsequent GPU passes operate in RGBA16F (half-float). The 5-stage color-transform chain (black balance → brightness → contrast → saturation → gamma) compounds quantization error at 8-bit but has effectively zero loss at half-float. `half` math runs full-rate on Apple Silicon. **Recording output:** HEVC 8-bit via a `rgba16f_to_yuv8` compute pass (device only supports 8-bit). **Still capture:** 8-bit 3-channel TIFF. Supersedes D-14. | Medium — the capture format is hardware-constrained (8-bit only on A16); the working format could be changed to 8-bit with a one-line Metal texture descriptor change, but re-introduces banding. |
| D-19 | **OpenCV packaged as a SwiftPM `binaryTarget`** wrapping `opencv2.xcframework`. | (A) Hand-wired `HEADER_SEARCH_PATHS` via `.xcconfig` (old D-05); (B) CocoaPods; (C) Build from source via CMake | Declarative dependency in `Package.swift`, no `.xcconfig` files, SwiftPM auto-selects device/simulator slices. Composes cleanly with `ImagingCore`'s SwiftPM target graph. The previous xcconfig approach would have forced Xcode-project-only builds and broken the headless CMake path for `ImagingCoreTests`. Refines D-05. | Low — switching to source build later (for binary size reduction) does not affect the `ImagingCore` interface. |
| D-20 | **Adopt `sending` (SE-0430) now** instead of `@unchecked Sendable` where possible. | (A) Keep `@unchecked Sendable` on transferable wrappers indefinitely; (B) Wait for Sendable conformances in Apple SDK | Previous design was tentative pending iOS 26 as the confirmed minimum. iOS 26 is now confirmed, Swift 6 is the language mode, and SE-0430 is stable. `sending` parameter annotations transfer ownership across actor boundaries without the `@unchecked Sendable` workaround. The narrow `IncomingFrame` wrapper over `CVPixelBuffer` is still marked `@unchecked Sendable` because `CVPixelBuffer` does not yet have a Foundation-provided `Sendable` conformance — this is documented as the one permitted exception in `02-concurrency.md §Sendable Strategy`. | Low — a `sending` parameter is strictly stronger than an `@unchecked Sendable` wrapper; dropping back to the wrapper is always possible. |
| D-21 | **`std::span<T>` across the C++ boundary** instead of raw `const uint8_t*` + length. | (A) Raw pointer + length pair; (B) `std::vector<T>` (deep-copies on iteration from Swift); (C) `UnsafeBufferPointer<T>` via C-ABI shim | `std::span` is non-escapable, bounds-checked, and imports into Swift as `Span<T>` under `.interoperabilityMode(.Cxx)` without heap allocation. This enforces the "valid only for duration of `onFrame()`" memory contract in the type system. The raw-pointer alternative was correct but easy to misuse. | Low — changing back to raw pointers is a header-only edit. |
| D-22 | **C++ exceptions caught at the `ImagingCore` public facade; translated to `ErrorCode` enum.** Every public method is `noexcept`. | (A) Let exceptions propagate and rely on Swift to not touch them; (B) Wrap each call site in Swift with Objective-C++ `@try/@catch`; (C) Use Swift 6's tentative `throws` importer for C++ | Uncaught C++ exceptions crossing into Swift abort the process. Swift 6's C++ `throws` importer is not yet production-ready for complex hierarchies like `cv::Exception`. Catching at the facade is deterministic: every public method has a `try { ... } catch (const cv::Exception&) { return ErrorCode::OpenCVFailure; } catch (const std::exception&) { return ErrorCode::InternalFailure; } catch (...) { return ErrorCode::InternalFailure; }` wrapper. The `noexcept` specifier asserts the contract at the type system level. This is enforced by code review and by a specific test in `ImagingCoreTests` that feeds a malformed image and asserts the facade returns an error code (rather than crashing). | Low — swapping in Swift's `throws` importer later would be additive if/when it stabilizes. |
| D-23 | **C++ `PixelSink` thread pool replaces Swift `ConsumerRegistry` actor.** `ImagingCore` owns a fixed-size C++ thread pool (`std::min(4, hw_concurrency)`) with per-stream MPSC lane queues and a **1-slot mailbox** (drop-on-busy, newest overwrites). Consumers subscribe to `PixelSink` and run entirely on pool threads. Supersedes D-10. | (A) Swift `ConsumerRegistry` actor with `DispatchQueue` per consumer (previous design); (B) Swift `AsyncStream.bufferingNewest(1)` per consumer; (C) C++ thread pool with 4-frame bounded queue (ADR §3.6) | All consumer threading is inside C++, which makes `ImagingCore` truly independently testable — the pool works the same whether called from Swift, gtest, or a future non-Apple platform. 1-slot mailbox (not 4-frame queue) is chosen for simplicity and strong liveness guarantees — the consumer always sees the newest frame, never a stale one. The Swift `ConsumerRegistry` actor is deleted; `PipelineKit` calls `PixelSink::publish()` directly from the GPU completion handler. | Medium — reverting to the Swift actor approach is possible but requires re-adding `ConsumerRegistry` and `DispatchQueue` per consumer. |
| D-24 | **Edge visualization: C++ composites edges onto tracker image, writes to shared MTLTexture.** Swift renders a fullscreen quad with pan/zoom transform. Intentionally less efficient than Metal-overlay compositing — mirrors future app development patterns where C++ will perform more complex overlays. | (A) SwiftUI `Canvas` `Path` rendering of contour list (original design); (B) Metal fragment shader overlay compositing (ADR §3.7); (C) Return binary mask, upload to GPU, fragment composite | C++ compositing is intentionally chosen despite being less efficient. The pattern — C++ receives pixels, processes, overlays, writes composited result — matches the architecture the app will evolve toward (C++ performing complex multi-layer overlays, stain normalization previews, ROI annotations). Building the inefficient version now validates the data path and avoids a future rearchitecture. The composited image is written to a shared MTLTexture (`.shared` storage mode with mipmap levels) so Swift can render it with pan/zoom. Mipmap levels give filtering quality when zoomed out despite the small (640×480) source resolution. The `EdgeResult` object separately carries a contour list for future ROI/annotation export. | Low — switching to Metal fragment overlay (B) or SwiftUI Canvas (A) requires only changing the rendering side; the C++ processing stays. |
| D-25 | **Center crop in Metal Pass 1.** Camera captures at full-sensor resolution (~4160×3120); Pass 1 crops to a user-defined region (e.g. 1600×1200) during the YUV→RGB conversion. All subsequent passes operate at crop resolution. | (A) Process at full sensor resolution throughout (previous design); (B) Capture at a lower AVFoundation preset | Full-sensor capture preserves fine detail via ~2.6× oversampling per axis. Cropping in Metal Pass 1 reduces the GPU workload for color transforms, readback, and encoding by ~6.5× versus full-sensor processing. The crop origin and size are compute uniforms, so the user can adjust the crop interactively. Capturing at a lower preset (B) throws away sensor data; cropping in Metal (A→chosen) preserves it for the crop operation and discards only the surplus. | Low — removing the crop (passing full-sensor dimensions as crop params) reverts to full-resolution processing. |
| D-26 | **`SWIFT_SHARED_REFERENCE` annotations on `PixelSink` and `EdgeDetector` C++ classes.** Swift imports them as ARC-managed reference types with explicit `retain_fn` / `release_fn` declarations. | (A) Import C++ classes as value types (implicit copy semantics); (B) Wrap in Objective-C++ classes for ARC bridging (old D-04 approach); (C) `@unchecked Sendable` Swift wrapper holding a raw pointer | `SWIFT_SHARED_REFERENCE` is the Swift 6.2 mechanism for importing C++ reference types with ARC ownership. It avoids copy semantics (option A, which would copy mutable state incorrectly for a class with identity), avoids ObjC++ (option B, deleted under D-16), and avoids the manual-lifetime pitfalls of option C. The retain/release functions are declared in the public header and implemented in the `.cpp` — straightforward, auditable. | Low — if `SWIFT_SHARED_REFERENCE` causes interop issues with a future Swift version, a thin `@unchecked Sendable` Swift wrapper is a drop-in fallback. |
| D-27 | **KVO → AsyncStream adapter** (`DeviceStateStream` in `CaptureKit`) wraps `AVCaptureDevice` KVO observations (ISO, exposureDuration, whiteBalanceGains, lensPosition, isAdjustingExposure) as `AsyncStream<CameraState.Snapshot>`. Consumed by `@MainActor CameraControlViewModel` in `EvaCore`. | (A) Engine-emitted `AsyncStream` populated by explicit method calls (previous design); (B) Combine `@Published` properties via KVO | KVO is AVFoundation's native change-notification mechanism for device state. Wrapping KVO directly keeps the adapter close to the source and avoids duplicating AVFoundation's own change-tracking. The engine's `AsyncStream` pattern (D-12) still applies for session-level events (state changes, errors, frame results) that don't map to device KVO — the two patterns coexist. The `DeviceStateStream` terminates cleanly on device reconfiguration and restarts on session resume. | Low — reverting to engine-emitted streams is a local change in `CaptureKit`. |
